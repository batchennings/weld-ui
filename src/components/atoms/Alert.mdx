import { Canvas, Controls, Meta, Story } from '@storybook/addon-docs';
import { Stories } from '@storybook/blocks';
import * as Alert from './Alert.stories';

<Meta title="Components/Alert" of={Alert} />

# Alert

Le composant `Alert` permet aux utilisateurs d'interagir avec une action cliquable, avec différents styles pour différentes hiérarchies d'action.

## Paramètres

Le composant `Alert` accepte les props suivantes pour personnaliser son apparence et son comportement :

<Controls />

## Exemples

<Stories />

