import { Canvas, Controls, Meta, Story } from '@storybook/addon-docs';
import { Stories } from '@storybook/blocks';
import * as Pill from './Pill.stories';

<Meta title="Components/Pill" of={Pill} />

# Pill

Le composant `Pill` permet aux utilisateurs d'interagir avec une action cliquable, avec différents styles pour différentes hiérarchies d'action.

## Paramètres

Le composant `Pill` accepte les props suivantes pour personnaliser son apparence et son comportement :

<Controls />

## Exemples

<Stories />

