import { Canvas, Controls, Meta, Story } from '@storybook/addon-docs';
import { Stories } from '@storybook/blocks';
import * as Button from './Button.stories';

<Meta title="Components/Button" of={Button} />

# Button

Le composant `Button` permet aux utilisateurs d'interagir avec une action cliquable, avec différents styles pour différentes hiérarchies d'action.

## Paramètres

Le composant `Button` accepte les props suivantes pour personnaliser son apparence et son comportement :

<Controls />

## Exemples

<Stories />

